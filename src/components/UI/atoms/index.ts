import Button from './button';
import Input from './input';
import Icon from './icon'
import Select from './select'

const Atoms = {
  Button,
  Input,
  Icon,
  Select,
};

export default Atoms;

export { Button,Input,Icon,Select };
