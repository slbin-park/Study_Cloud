import Button from './button';
import Input from './input';
import Icon from './icon'
import Select from './select'
import Modal from './modal'
const Atoms = {
  Button,
  Input,
  Icon,
  Select,
  Modal,
};

export default Atoms;

export { Button,Input,Icon,Select,Modal };
